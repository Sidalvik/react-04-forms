import React from 'react';
import PropTypes from 'prop-types';

function PhotoImg(props) {
  return (
    <div className='PhotoImg'>PhotoImg</div>
  )
}

PhotoImg.propTypes = {
    props: PropTypes.any,
}

export default PhotoImg
